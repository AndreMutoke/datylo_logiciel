#include "runningMenu.h"
#include "util.h"

extern MENU menuSelect;
extern MODE modeJeuxSYsteme;
extern SDL_Event event;

void navigationMenu()
{
    
}
void runningMenu()
{

    if(modeJeuxSYsteme == MODE_JOUER )
    {
        // runninggame();
    }

    if(modeJeuxSYsteme == MODE_AIDE)
    {
        // runninggame();
    }
    if(modeJeuxSYsteme == MODE_QUITTER)
    {
        // runninggame();
    }

}